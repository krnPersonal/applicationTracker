app.jwt.secret=${JWT_SECRET_KEY}
spring.datasource.url=${DB_URL:jdbc:mysql://mysql:3306/applicationtracker}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173}}
app.usajobs.user-agent=${USAJOBS_USER_AGENT:}
app.usajobs.api-key=${USAJOBS_API_KEY:}
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
